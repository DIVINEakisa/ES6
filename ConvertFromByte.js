//write the function that will return the given byte into its original number
// Example: 111 will return 7
let convertIntoBytes = (byte) => {
  let originalNumber = parseInt(byte, 2);
  return originalNumber;
};
console.log(convertIntoBytes(111));
